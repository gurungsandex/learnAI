# Leave blank to run fully client-only (current default).
# Once the backend in server/ is deployed, set this to its base URL
# (e.g. https://api.learnai.example.com) to enable GameContext's
# background sync in src/context/GameContext.jsx.
VITE_API_URL=
